/**
 * Controllers Index - 控制器导出
 */

const authController = require('./authController');
const bookmarkController = require('./bookmarkController');

module.exports = {
  authController,
  bookmarkController,
};